# ColorTabs

Firefox extension that colors the browser window based on the current domain.

Useful to instantly tell apart environments — production, staging, local — without reading the URL. Each domain or regex pattern gets its own color, applied to the browser chrome in real time.
